# Linear-only baseline for chain-structured tasks.


num_classes: 10

backbone:
  type: config
  layers:
    - {type: linear, in_features: 10, out_features: 10}     # MUST equal num_classes

graph:
  type: chain
  seq_len: 30

pairwise:
  init_scale: 0.1
